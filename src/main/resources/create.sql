CREATE TABLE %s (id INT PRIMARY KEY AUTO_INCREMENT, inserted TIMESTAMP DEFAULT CURRENT_TIMESTAMP, tape INT, filename VARCHAR(255) NOT NULL, size INT, ctime TIMESTAMP , mtime TIMESTAMP, qhash VARCHAR(40));